#include <iostream>

int main()
{
	// !   = not(non) inversela valeur logique
	// ||  = or(ou) retourne true si au moins un des critères est satisfait 
	// &&  = and(et) retourne true si tout les critères sont satisfait

	//Exemples:
	//if(x > 0 && x < 30) si x est suppérieur a 0 ET inférieur à 30 
	//if(x == 5 || y == 8) si x est égale a 5 OU si y est égale à 8
	//bool sunny = true; (il fait soleil)
	//if(!sunny) si sunny est false

	return 0;
}