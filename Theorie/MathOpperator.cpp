#include <iostream>

int main()
{
	//Opperation mathematique (+ - * /)
	int student = {20};
	
	//addition (+)
	//student += 1; / student = student +1; (le 1 peux etre changer pour la valeur que vous souaiter additioner a votre varriable)
	//student++; (cette versiont est appelée incrémentation, elle augmente la valeur de la variable de 1 et est majoritairement utilisée dans des boucle)

	//soustraction (-)
	//student -= 1; / student = student -1;
	//student--;

	//multiplication(*)
	//student *= 2; / student = student * 2;
	
	//division(/)
	//student /= 2; / student = student / 2;

	return 0; 
}